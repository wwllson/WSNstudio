<?php
/* ============================================================
   mail-secret.php — holds ONLY your mailbox password.

   1) Copy this file and rename the copy to  mail-secret.php
      (same folder as contact.php).
   2) Replace the placeholder below with your NEW
      contact@wsnstudio.co.uk mailbox password.
   3) NEVER commit mail-secret.php to Git — it is already listed
      in .gitignore. The safest way is to create it directly on
      the server via Hostinger's File Manager after deploying.
   ============================================================ */

return 'PUT-YOUR-NEW-MAILBOX-PASSWORD-HERE';
